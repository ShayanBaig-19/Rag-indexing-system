# Rag-indexing-system
A Python-based RAG indexing pipeline that extracts text from PDFs, performs intelligent chunking, generates semantic embeddings, and stores vector embeddings in Pinecone for AI-powered retrieval.
